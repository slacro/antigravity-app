{
  "name": "crypto-dashboard-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start:server": "npm run start --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev": "concurrently \"npm run start:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
